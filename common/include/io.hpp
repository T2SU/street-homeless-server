//
// Created by TSU on 2023-05-11.
//

#ifndef STREET_HOMELESS_SERVER_IO_HPP
#define STREET_HOMELESS_SERVER_IO_HPP

#if defined(_MSC_VER) ||                                            \
    (defined(__GNUC__) && (__GNUC__ == 3 && __GNUC_MINOR__ >= 4) || \
     (__GNUC__ >= 4))  // GCC supports "pragma once" correctly since 3.4
#pragma once
#endif

#include "io/io_concept.hpp"
#include "io/in_buffer.hpp"
#include "io/out_buffer.hpp"

#endif //STREET_HOMELESS_SERVER_IO_HPP
